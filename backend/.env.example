# Datadog Marketplace — Backend Environment Variables
# Copy this to .env and fill in your values.
# Most DD_* vars are set by start.sh — you only need DATABASE_URL here.

DATABASE_URL=postgresql://ddstore_app:your-password@localhost:5432/ddstore
